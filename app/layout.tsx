import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from '@/components/providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Alternative 3D Studio | Professional 3D Services',
  description: 'Transform your ideas into stunning 3D visualizations. Professional 3D modeling, rendering, and animation services.',
  keywords: ['3D studio', '3D modeling', 'rendering', 'animation', 'visualization'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
